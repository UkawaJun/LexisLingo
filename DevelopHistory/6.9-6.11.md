
## 2026-06-09

### DailyWordData 参数重新定义
- 字段语义最终确认：`t`=学习时长(分钟) `x`=听力次数 `n`=练习词卡次数 `ex`=单元考核次数 `ee`=单元考核错误次数 `re`=艾宾浩斯复习次数(预留) `er`=艾宾浩斯复习错误次数(预留)
- 新增 `ee` 字段，模板 `DIC_LABEL["DailyWordData"]` 补充 `"ee": 0`
- `re/er` 不再在单元考核中写入，保留给艾宾浩斯模块

### ExamDialog._finish 清算逻辑修正
- `WrongTimes += wrong`（错误题目数，原来固定+1）
- `TestTimes += correct`（正确题目数，原来固定+1）
- 不论通过与否都累加 `WrongTimes` 和 `TestTimes`，只有通过才更新 `IsExamed/LastReviewTime/ReviewTimes/FirstSeenAt`
- 每日统计：`ex` 加总题数，`ee` 加错误题数，`n` 加总题数
- 删除原来的 `re/er` 写入

### 混合模式 QuizDialog 数据写回修正
- 暴露度改为 `round(15 * (correct / total), 2)`
- `n` 只加答对数（防刷）
- `x` 加听力题数量（`audio_spell` + `audio_trans`）
- 不再写入 `ex/ee/re/er`

### 模式1/2 暴露度修正
- 暴露度改用 `SLEL["mode1"]`（模式1）和 `SLEL["mode2"]`（模式2）权重
- `n` 只加答对数
- `x` 加总题数（每题都播放了音频）
- 合并循环，一次读库一次写库

### LastLoginAt
- 程序正常退出时写入当前时间戳到 `_UserCDict["LastLoginAt"]`

### CurrentCourse
- 章节考核全部词通过后，更新 `CurrentCourse = f"{NowState['M']}_{NowState['N']}_{NowState['S']}"`

### 撇号词处理
- 初始化词库时（`_lessonList` 循环）：`page[4].replace(' ','') == "'"` 则 `continue` 跳过，不构建也不计词数
- `load_article` 里：单独撇号词替换为 `'s`
- `list_red/list_blue` 构建正则前过滤掉单独撇号词
- `_safe_filename` 里：`'s` 替换为 `s`，单独 `'` 替换为 `s`，空文件名用哈希兜底

### load_chapters_by_module 性能优化
- 去掉 `BackFromWordSqlite` 词形还原（`_W` 里的词本来就是还原好的）
- `has()` + `get()` 两次查库合并为一次 `get()`，返回 `None` 即不存在
- 使用 `GLO_COU["_W"]` 存储词表，`_W.clear()` 前先 `GLO_COU["_W"] = dict(_W)`
- 136课模块从3秒卡顿变流畅

### UpdatePerTime 3分钟自动保存 + 23:55自动关闭
- `QTimer` 每3分钟触发：计算 `elapsed_minutes` 增量写入 `t`，更新 `LastLoginAt`，写库
- 触发后检测当前时间，23:55之后弹出10秒倒计时窗口，倒计时结束后 `QApplication.quit()`
- 55分钟设定保证3分钟间隔必然落入55-59区间，无跨天问题
- `APP_Time[0]` 每次触发后重置，`APP_Time[1]` 同步更新为今日累计时长

### 异常崩溃保存
- `sys.excepthook` 钩子：未捕获异常时先执行 `_on_app_quit()` 写库再打印 traceback

### Debug30Days 增强
- 左侧新增课程进度折叠按钮（▼/▶），点击收起/展开课程和当前进度两行
- 火焰动画：`🔥🔆✨` 每400ms切换，显示连续学习天数（`t > 1` 才算有效天）
- 载入前隐藏火焰和天数标签，载入后显示
- 实时时钟：Consolas字体，每秒更新
- 截图按钮：保存到 `Report/` 文件夹，命名 `用户_日期时间_学习数据.png`
- 左侧连续为零的天数自动裁掉，图表从第一个有数据的天开始显示
- 新增 `ee` 字段，按钮分三组用竖线分隔：`时长/听力/练习` | `考核次数/考核错误` | `复习次数/复习错误` | `新学单词`

### Debug90Days + Debug12Months 新增
- `Debug90Days.py` + `show90DaysDate.html`：90天版，天为单位，逻辑与30天一致
- `Debug12Months.py` + `show12MonthsDate.html`：12个月版，月为单位，每月所有天数据加总
- 12个月版无连续天数显示，新学单词按月统计 `FirstSeenAt`
- HTML均放于 `HTML/` 文件夹
